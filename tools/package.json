{
  "name": "@signalai/tools",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "echo 'no automated tests in tools/ — chat-cli.ts is a manual two-terminal demo' && exit 0",
    "chat": "tsx chat-cli.ts"
  },
  "dependencies": {
    "@signalai/client-sdk": "workspace:*"
  }
}
